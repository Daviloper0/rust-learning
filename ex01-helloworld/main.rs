fn main() { // Primeiro código que roda em todo programa RUST (como em C)
    println!("Hello, world!"); // Isso é um macro, por que? Eu não faço ideia (eu sei que é por causa do !), mas por que não existe um println em rust?
}
